# NewQuest

Follow `.cursor/rules/project-preferences.mdc` for the user's standing preferences.
Use Roblox's published Luau style guide and `.stylua.toml` for project code.
Run `selene src` after every change; new code must have no Selene errors or warnings.
The user handles commits and pushes manually; do neither unless asked again.
The final destination is single-player and part of Berry Avenue's universe.
Studio playtests must use ProfileStore.Mock, preserving live player data.
